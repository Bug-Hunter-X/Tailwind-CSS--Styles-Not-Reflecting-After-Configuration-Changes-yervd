```javascript
// No changes are needed in your JavaScript code. The solution is in your build process.
// Ensure that after making changes to your tailwind.config.js, you run the build process of your framework
// e.g npm run build, yarn build or the appropriate command for your project.
```

```html
<!-- In your HTML file. No changes needed here. -->
<div class="bg-red-500 p-4">
  This should be red
</div>
```